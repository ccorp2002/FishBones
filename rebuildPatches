#!/bin/bash

set -e

force_run=false

if [[ "$1" == "--force" ]]; then
  force_run=true
  echo "Force mode enabled. All Gradle tasks will run."
fi

echo "Processing file patches..."

declare -A gradle_tasks

process_changes() {
  local dir="$1"
  local project="$2"

  if [ ! -d "$dir" ]; then
    echo "Error: The directory '$dir' does not exist or is not valid."
    exit 1
  fi

  cd "$dir"

  if $force_run || ! git diff --quiet || ! git diff --cached --quiet; then
    echo "Changes detected in $dir (or force mode enabled). Running Gradle fixup and rebuild tasks."
    gradle_tasks["fixup${project}FilePatches"]="true"
    gradle_tasks["rebuild${project}FilePatches"]="true"
  else
    echo "No changes detected in $dir"
  fi

  cd - > /dev/null
}

run_gradle_task() {
  local task="$1"
  if [ "${gradle_tasks[$task]}" = "true" ]; then
    echo "Running Gradle task: $task"
    ./gradlew  --no-daemon -Dorg.gradle.java.home=/home/ccorp2002/JVMs/graalvm-jdk-23/ "$task" || echo "Gradle task '$task' failed, continuing..."
    echo "Gradle task '$task' completed (or failed but continuing)."
  else
    echo "Skipping Gradle task '$task' as no changes were detected."
  fi
}

process_changes "./pufferfish-server/" "PufferfishServer"
process_changes "./pufferfish-api/" "PufferfishApi"
process_changes "./paper-server/" "PaperServer"
process_changes "./paper-api/" "PaperApi"
process_changes "./fish-server/src/minecraft/java" "Minecraft"

gradle_rebuild_task=false

if $force_run || ! git diff --quiet "./fish-server/build.gradle.kts" || ! git diff --cached --quiet "./Purpur-server/build.gradle.kts"; then
  echo "Changes detected in ./fish-server/build.gradle.kts"
  gradle_rebuild_task=true
fi

if $force_run || ! git diff --quiet "./Purpur-api/build.gradle.kts" || ! git diff --cached --quiet "./Purpur-api/build.gradle.kts"; then
  echo "Changes detected in ./fish-api/build.gradle.kts"
  gradle_rebuild_task=true
fi

if $gradle_rebuild_task; then
  gradle_tasks["rebuildPufferfishSingleFilePatches"]="true"
fi

echo "Running fixup tasks..."
run_gradle_task "fixupPufferfishApiFilePatches"
run_gradle_task "fixupPaperApiFilePatches"
run_gradle_task "fixupPufferfishServerFilePatches"
run_gradle_task "fixupPaperServerFilePatches"
run_gradle_task "fixupMinecraftFilePatches"

echo "Running rebuild tasks..."
run_gradle_task "rebuildPufferfishApiFilePatches"
run_gradle_task "rebuildPaperApiFilePatches"
run_gradle_task "rebuildPufferfishServerFilePatches"
run_gradle_task "rebuildPaperServerFilePatches"
run_gradle_task "rebuildMinecraftFilePatches"
run_gradle_task "rebuildPufferfishSingleFilePatches"
